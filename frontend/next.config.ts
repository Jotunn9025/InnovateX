/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.imgur.com",
      "example.com",
      "res.cloudinary.com", 
    ],
  },
};

module.exports = nextConfig;
